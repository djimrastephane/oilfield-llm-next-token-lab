torch>=2.0.0  # register_forward_pre_hook(..., with_kwargs=True) requires 2.0+; used in advanced notebooks 7-8
transformers>=4.56.0  # from_pretrained(..., dtype=...) requires 4.56+; older versions only accept torch_dtype=
accelerate
pandas>=1.4.0  # DataFrame.style.hide(axis="index") requires 1.4+; older versions only have hide_index()
matplotlib
numpy
jupyter
scikit-learn
